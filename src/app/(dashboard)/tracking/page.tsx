const TrackingPage = () => {
  // TODO: If the user is not logged in, redirect to the login page
  // TODO: If the user is logged in but has not verified their email, redirect to the verify email page
  // TODO: If the user is logged in and has verified their email, but did not complete account setup, redirect to the account setup page
  return (
    <div>
      <h1>Tracking</h1>
      <p>Welcome to the tracking page</p>
    </div>
  );
};

export default TrackingPage;
